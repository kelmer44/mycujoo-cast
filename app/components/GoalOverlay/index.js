import React, { PureComponent } from 'react'
import styles from '/GoalOverlay.css'

export default class GoalOverlay {
    render() {
        return (
            <div className={styles.root}>test</div>
        )
    }
}
